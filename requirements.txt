fastapi>=0.110.0
uvicorn[standard]>=0.29.0
deepgram-sdk>=3.5.0
python-multipart>=0.0.9
websockets>=12.0

# Resume Screening pipeline
pdfplumber>=0.11.0       # PDF text extraction
ollama>=0.2.0            # llama3.2:3b (extraction) + nomic-embed-text (embeddings)
numpy>=1.26.0            # cosine similarity

python-dotenv>=1.0.1
# Optional -- only needed for CLI mode (no server microphone needed for FastAPI mode)
# pyaudio>=0.2.14